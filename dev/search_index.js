var documenterSearchIndex = {"docs":
[{"category":"section","location":"references/#References","page":"References","text":"The bounded set of sources this project works from. The survey step of every feature consults these and only these; adding a source is a deliberate decision recorded here with a reason, not an ambient accumulation. The collection below is sufficient for every phase of the roadmap.","title":"References"},{"category":"section","location":"references/#Literature","page":"References","text":"Saad, Iterative Methods for Sparse Linear Systems (2nd ed.) — freely available from the author; the reference for sparse formats, sparse matrix-vector products, Krylov methods, and the ic0/ilu0 preconditioners of Phase 6.\nGolub & Van Loan, Matrix Computations (4th ed.), ch. 11 — Krylov methods from the dense-linear-algebra side; shared shelf with AlgorithmicNLA.jl.","title":"Literature"},{"category":"section","location":"references/#Code-and-documentation","page":"References","text":"SparseArrays (stdlib) — source and documentation; defines the user-facing API and the semantics of structural versus numerical zeros. Its test suite is the model for what to validate.\nCUDA.jl's CUSPARSE wrappers and the NVIDIA cuSPARSE documentation — define the device side: formats, naming, index types, and which operations each format supports; the documented behavior is the parity target.\nMetal.jl and the Metal Performance Shaders documentation — what the hardware and Apple's libraries actually provide; re-checked each audit-code pass because MPS coverage grows by release.\nKrylov.jl and KrylovPreconditioners.jl — the operator and preconditioner interfaces Phase 4 and Phase 6 must satisfy; their docs state the required methods precisely.","title":"Code and documentation"},{"category":"section","location":"references/#Deliberately-not-in-the-set","page":"References","text":"Davis, Direct Methods for Sparse Linear Systems — direct sparse factorization is a stated non-goal of the roadmap. General GPU-computing texts add nothing over the Metal.jl/MPS documentation for this package's needs. Grow this page only when a phase demonstrably needs a source the set lacks.","title":"Deliberately not in the set"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"api/#Utilities","page":"API reference","text":"","title":"Utilities"},{"category":"module","location":"api/#MetalSparseArrays.MetalSparseArrays","page":"API reference","text":"MetalSparseArrays\n\nSparse arrays for the Apple Silicon GPU backend.\n\nThe package provides the sparse storage formats, array interface, and sparse linear algebra kernels that SparseArrays provides on the CPU and that CUDA.CUSPARSE provides on NVIDIA hardware, so that code written against either runs on Metal, including the operator interface required by iterative solvers such as Krylov.\n\nStorage formats and the operations on them are organized one per file, included below.\n\n\n\n\n\n","title":"MetalSparseArrays.MetalSparseArrays"},{"category":"type","location":"api/#MetalSparseArrays.AbstractMtlSparseMatrix","page":"API reference","text":"AbstractMtlSparseMatrix{Tv, Ti} <: SparseArrays.AbstractSparseMatrix{Tv, Ti}\n\nSupertype of the device-resident sparse matrix formats. Tv is the element type of the stored values and Ti <: Integer the type of the index arrays. Every format stores its index and value arrays as MtlVectors and follows the semantics of SparseArrays: a \"nonzero\" is a stored entry, which may hold the value zero.\n\n\n\n\n\n","title":"MetalSparseArrays.AbstractMtlSparseMatrix"},{"category":"type","location":"api/#MetalSparseArrays.DEFAULT_INDEX_TYPE","page":"API reference","text":"DEFAULT_INDEX_TYPE\n\nThe integer type used for row, column, and pointer arrays of device-resident sparse matrices, Int32. This matches the index type of CUDA.CUSPARSE and avoids the memory and bandwidth cost of 64-bit indices, at the cost of restricting a stored dimension or nonzero count to typemax(Int32). SparseArrays.SparseMatrixCSC defaults to Int on the CPU, so conversion between host and device changes Ti.\n\n\n\n\n\n","title":"MetalSparseArrays.DEFAULT_INDEX_TYPE"},{"category":"type","location":"api/#MetalSparseArrays.MtlSparseMatrixCOO","page":"API reference","text":"MtlSparseMatrixCOO{Tv, Ti} <: AbstractMtlSparseMatrix{Tv, Ti}\n\nDevice-resident sparse matrix in coordinate storage, corresponding to CUDA.CUSPARSE.CuSparseMatrixCOO. An m-by-n matrix with k stored entries holds three MtlVectors of length k: rowval and colval with the row and column index of each entry, and nzval with its value.\n\nFollowing CUSPARSE, entries are sorted lexicographically by (row, column) and coordinates are not duplicated; like the sortedness assumption of the compressed formats, this is documented but not validated. Assembly from arbitrary, duplicated coordinates is the job of sparse(I, J, V, m, n), not of this constructor.\n\nConstruct from a SparseMatrixCSC — MtlSparseMatrixCOO(A) with index type DEFAULT_INDEX_TYPE, or MtlSparseMatrixCOO{Tv, Ti}(A) — or from raw device arrays with MtlSparseMatrixCOO(m, n, rowval, colval, nzval), which validates that the three arrays have equal length and every index is in range, and throws ArgumentError naming the violated invariant otherwise.\n\nStored entries with the value zero are preserved, matching SparseArrays. Convert back with SparseMatrixCSC(A) or adapt(Array, A); the round trip is exact.\n\n\n\n\n\n","title":"MetalSparseArrays.MtlSparseMatrixCOO"},{"category":"method","location":"api/#MetalSparseArrays.MtlSparseMatrixCOO-Union{Tuple{MtlSparseMatrixCSC{Tv, Ti}}, Tuple{Ti}, Tuple{Tv}} where {Tv, Ti}","page":"API reference","text":"MtlSparseMatrixCOO(A::MtlSparseMatrixCSC)\n\nConvert to COO. This reorders every entry from column-major to the row-major order COO stores, and is currently computed through the host SparseMatrixCSC fallback (a full transfer each way); a device reorder kernel is planned with the Phase 3 kernel infrastructure. The (i, j, v) triple set is preserved exactly.\n\n\n\n\n\n","title":"MetalSparseArrays.MtlSparseMatrixCOO"},{"category":"method","location":"api/#MetalSparseArrays.MtlSparseMatrixCOO-Union{Tuple{MtlSparseMatrixCSR{Tv, Ti}}, Tuple{Ti}, Tuple{Tv}} where {Tv, Ti}","page":"API reference","text":"MtlSparseMatrixCOO(A::MtlSparseMatrixCSR)\n\nConvert to COO. Both formats store entries in row-major order, so the column index and value arrays are copied on the device and the row indices are expanded on the host from an O(m) transfer of the row pointer; no value touches the host. The (i, j, v) triple set is preserved exactly.\n\n\n\n\n\n","title":"MetalSparseArrays.MtlSparseMatrixCOO"},{"category":"type","location":"api/#MetalSparseArrays.MtlSparseMatrixCSC","page":"API reference","text":"MtlSparseMatrixCSC{Tv, Ti} <: AbstractMtlSparseMatrix{Tv, Ti}\n\nDevice-resident sparse matrix in compressed sparse column storage, mirroring SparseMatrixCSC and corresponding to CUDA.CUSPARSE.CuSparseMatrixCSC. An m-by-n matrix with k stored entries holds three MtlVectors: colptr of length n + 1 with colptr[j]:colptr[j + 1] - 1 the range of entries of column j (colptr[1] == 1, monotonically nondecreasing, colptr[n + 1] == k + 1), rowval of length k with the row index of each entry, and nzval of length k with its value.\n\nConstruct from a SparseMatrixCSC — MtlSparseMatrixCSC(A) with index type DEFAULT_INDEX_TYPE, or MtlSparseMatrixCSC{Tv, Ti}(A); the transfer copies the three arrays directly, with no reordering — or from raw device arrays with MtlSparseMatrixCSC(m, n, colptr, rowval, nzval), which validates the invariants above and that every row index lies in 1:m, and throws ArgumentError naming the violated invariant otherwise. Row indices within a column are assumed sorted ascending without duplicates and this is not validated, matching SparseMatrixCSC. Unlike SparseMatrixCSC, the index and value arrays must have exactly their required lengths — oversized buffers are not accepted.\n\nStored entries with the value zero are preserved, matching SparseArrays. Convert back with SparseMatrixCSC(A) or adapt(Array, A); the round trip is exact. adapt(MtlArray, A) on a host SparseMatrixCSC produces this format.\n\n\n\n\n\n","title":"MetalSparseArrays.MtlSparseMatrixCSC"},{"category":"method","location":"api/#MetalSparseArrays.MtlSparseMatrixCSC-Union{Tuple{MtlSparseMatrixCSR{Tv, Ti}}, Tuple{Ti}, Tuple{Tv}} where {Tv, Ti}","page":"API reference","text":"MtlSparseMatrixCSC(A::MtlSparseMatrixCSR)\nMtlSparseMatrixCSC(A::MtlSparseMatrixCOO)\n\nConvert to CSC. Both reorder every entry from row-major to column-major, and are currently computed through the host SparseMatrixCSC fallback (a full transfer each way); a device reorder kernel is planned with the Phase 3 kernel infrastructure. The (i, j, v) triple set is preserved exactly.\n\n\n\n\n\n","title":"MetalSparseArrays.MtlSparseMatrixCSC"},{"category":"type","location":"api/#MetalSparseArrays.MtlSparseMatrixCSR","page":"API reference","text":"MtlSparseMatrixCSR{Tv, Ti} <: AbstractMtlSparseMatrix{Tv, Ti}\n\nDevice-resident sparse matrix in compressed sparse row storage, the primary device format, corresponding to CUDA.CUSPARSE.CuSparseMatrixCSR. An m-by-n matrix with k stored entries holds three MtlVectors: rowptr of length m + 1 with rowptr[i]:rowptr[i + 1] - 1 the range of entries of row i (rowptr[1] == 1, monotonically nondecreasing, rowptr[m + 1] == k + 1), colval of length k with the column index of each entry, and nzval of length k with its value.\n\nConstruct from a SparseMatrixCSC — MtlSparseMatrixCSR(A) with index type DEFAULT_INDEX_TYPE, or MtlSparseMatrixCSR{Tv, Ti}(A) — or from raw device arrays with MtlSparseMatrixCSR(m, n, rowptr, colval, nzval), which validates the invariants above and that every column index lies in 1:n, and throws ArgumentError naming the violated invariant otherwise. Column indices within a row are assumed sorted ascending without duplicates and this is not validated, matching the assumption SparseMatrixCSC makes for its row indices. Unlike SparseMatrixCSC, the index and value arrays must have exactly their required lengths — oversized buffers are not accepted, so the stored arrays always mean what the invariants say.\n\nStored entries with the value zero are preserved, matching SparseArrays. Convert back with SparseMatrixCSC(A) or adapt(Array, A); the round trip is exact, including the sparsity pattern and index order.\n\n\n\n\n\n","title":"MetalSparseArrays.MtlSparseMatrixCSR"},{"category":"method","location":"api/#MetalSparseArrays.MtlSparseMatrixCSR-Union{Tuple{MtlSparseMatrixCOO{Tv, Ti}}, Tuple{Ti}, Tuple{Tv}} where {Tv, Ti}","page":"API reference","text":"MtlSparseMatrixCSR(A::MtlSparseMatrixCOO)\n\nConvert to CSR. Both formats store entries in row-major order, so the column index and value arrays are copied on the device and only the row pointer is built on the host from an O(nnz) transfer of the row indices; no value touches the host. The (i, j, v) triple set is preserved exactly.\n\n\n\n\n\n","title":"MetalSparseArrays.MtlSparseMatrixCSR"},{"category":"method","location":"api/#MetalSparseArrays.MtlSparseMatrixCSR-Union{Tuple{MtlSparseMatrixCSC{Tv, Ti}}, Tuple{Ti}, Tuple{Tv}} where {Tv, Ti}","page":"API reference","text":"MtlSparseMatrixCSR(A::MtlSparseMatrixCSC)\n\nConvert to CSR. This reorders every entry from column-major to row-major, and is currently computed through the host SparseMatrixCSC fallback (a full transfer each way); a device reorder kernel is planned with the Phase 3 kernel infrastructure. The (i, j, v) triple set is preserved exactly.\n\n\n\n\n\n","title":"MetalSparseArrays.MtlSparseMatrixCSR"},{"category":"method","location":"api/#SparseArrays.SparseMatrixCSC-Union{Tuple{MtlSparseMatrixCOO{Tv, Ti}}, Tuple{Ti}, Tuple{Tv}} where {Tv, Ti}","page":"API reference","text":"SparseMatrixCSC(A::MtlSparseMatrixCOO{Tv, Ti}) -> SparseMatrixCSC{Tv, Ti}\n\nThe host CSC matrix equal to A, assembled with sparse(I, J, V, m, n) from host copies of the coordinate arrays. Under the format's no-duplicates invariant the conversion is exact, stored zeros included.\n\n\n\n\n\n","title":"SparseArrays.SparseMatrixCSC"},{"category":"method","location":"api/#SparseArrays.SparseMatrixCSC-Union{Tuple{MtlSparseMatrixCSC{Tv, Ti}}, Tuple{Ti}, Tuple{Tv}} where {Tv, Ti}","page":"API reference","text":"SparseMatrixCSC(A::MtlSparseMatrixCSC{Tv, Ti}) -> SparseMatrixCSC{Tv, Ti}\n\nThe host CSC matrix equal to A: the three storage arrays copied to the host unchanged. The conversion is exact, so converting a SparseMatrixCSC to the device and back is the identity up to the index type.\n\n\n\n\n\n","title":"SparseArrays.SparseMatrixCSC"},{"category":"method","location":"api/#SparseArrays.SparseMatrixCSC-Union{Tuple{MtlSparseMatrixCSR{Tv, Ti}}, Tuple{Ti}, Tuple{Tv}} where {Tv, Ti}","page":"API reference","text":"SparseMatrixCSC(A::MtlSparseMatrixCSR{Tv, Ti}) -> SparseMatrixCSC{Tv, Ti}\n\nThe host CSC matrix equal to A. The conversion is exact: the sparsity pattern, the ordering of the index arrays, and every stored value (zeros included) are reproduced, so converting a SparseMatrixCSC to the device and back is the identity up to the index type.\n\n\n\n\n\n","title":"SparseArrays.SparseMatrixCSC"},{"category":"method","location":"api/#Adapt.adapt_storage-Tuple{Type{Metal.MtlArray}, SparseArrays.SparseMatrixCSC}","page":"API reference","text":"Adapt.adapt_storage(::Type{MtlArray}, A::SparseMatrixCSC)\n\nadapt(MtlArray, A) transfers a host SparseMatrixCSC to the device as an MtlSparseMatrixCSC with index type DEFAULT_INDEX_TYPE, preserving the storage format, exactly as CUDA.CUSPARSE adapts to CuArray.\n\nThis method is deliberate type piracy — both MtlArray and SparseMatrixCSC are owned by other packages — committed knowingly on the CUSPARSE precedent and excepted in the Aqua piracy check; it is the one pirated method this package defines.\n\n\n\n\n\n","title":"Adapt.adapt_storage"},{"category":"method","location":"api/#MetalSparseArrays.compressed_check-Union{Tuple{Ti}, Tuple{Integer, Integer, Metal.MtlVector{Ti}, Metal.MtlVector{Ti}, Metal.MtlVector, String, String}} where Ti<:Integer","page":"API reference","text":"compressed_check(major, minor, ptr, idx, nzval, ptrname, idxname)\n\nValidate the compressed-storage invariants shared by the CSR (major == m) and CSC (major == n) formats: ptr has length major + 1, starts at one, and is monotonically nondecreasing; idx and nzval have exactly ptr[end] - 1 entries; and every index in idx lies in 1:minor. Throws ArgumentError naming the violated invariant, with ptrname/idxname naming the arrays in the format's own vocabulary. The pointer invariants are checked on a host copy of ptr (an O(major) transfer, accepted at construction time); the index range is checked by a device reduction, so idx is never transferred. Sortedness within a major slice is an unchecked documented assumption, matching SparseArrays.sparse_check.\n\n\n\n\n\n","title":"MetalSparseArrays.compressed_check"},{"category":"method","location":"api/#MetalSparseArrays.indextype-Union{Tuple{SparseArrays.AbstractSparseArray{<:Any, Ti}}, Tuple{Ti}} where Ti","page":"API reference","text":"indextype(A)\n\nThe integer type Ti used for the index arrays of the sparse array A.\n\n\n\n\n\n","title":"MetalSparseArrays.indextype"},{"category":"method","location":"api/#MetalSparseArrays.realtype-Union{Tuple{Type{T}}, Tuple{T}} where T<:Number","page":"API reference","text":"realtype(T)\n\nThe real floating point type used for magnitudes of elements of type T, i.e. real(float(T)). Used for norms, tolerances, and convergence criteria.\n\n\n\n\n\n","title":"MetalSparseArrays.realtype"},{"category":"method","location":"api/#MetalSparseArrays.unit_roundoff-Union{Tuple{Type{T}}, Tuple{T}} where T<:Number","page":"API reference","text":"unit_roundoff(T)\n\nThe unit roundoff u = eps(realtype(T)) / 2 of the floating point type associated with T.\n\n\n\n\n\n","title":"MetalSparseArrays.unit_roundoff"},{"category":"method","location":"api/#SparseArrays.nonzeros-Tuple{AbstractMtlSparseMatrix}","page":"API reference","text":"nonzeros(A::AbstractMtlSparseMatrix)\n\nThe MtlVector of stored values of A, in the storage order of the format (row-major for CSR and COO, column-major for CSC), aliasing the matrix. Stored entries may hold the value zero.\n\n\n\n\n\n","title":"SparseArrays.nonzeros"},{"category":"section","location":"changelog/#Changelog","page":"Changelog","text":"All notable changes to this package are documented in this file.\n\nThe format is based on Keep a Changelog, and this package adheres to Semantic Versioning.","title":"Changelog"},{"category":"section","location":"changelog/#[Unreleased]","page":"Changelog","text":"Initial development. Package scaffolding, the device-guarded test harness validating against the SparseArrays CPU reference, CI (tests, Aqua QA, ExplicitImports, Runic formatting, docs, benchmarks), and shared utilities (DEFAULT_INDEX_TYPE, indextype, realtype, unit_roundoff).","title":"[Unreleased]"},{"category":"section","location":"roadmap/#Roadmap","page":"Roadmap","text":"A coarse ordering of the work. Phases are sequenced so that each one supplies what the next one needs; within a phase, features are planned and implemented one at a time via the plan-feature workflow.\n\nThe target API is the union of what SparseArrays provides on the CPU and what CUDA.CUSPARSE provides on NVIDIA hardware. Code written against either should run unchanged on Metal.\n\nEach phase lists its deliverables — the concrete types and functions it adds — and a phase gate: the test criteria that define completion. The gates are a developer contract: a phase is complete only when its gate passes in CI, a gate may be strengthened but never weakened once written, and a feature that cannot meet its gate is redesigned, not exempted. Throughout, u denotes the unit roundoff of the element type and c a modest constant; \"reference\" means the SparseArrays result for the same input on the CPU. Structural results (patterns, index arrays, nnz) must match the reference exactly; floating point results to the stated bound.","title":"Roadmap"},{"category":"section","location":"roadmap/#Cross-cutting-requirements","page":"Roadmap","text":"These apply to every phase rather than being phases themselves:\n\nEvery device result is validated against the CPU reference as part of the feature, never retroactively. Every gate below runs for every format it applies to, every supported element type, both index types (Int32, Int64), and with Metal.allowscalar(false) in force.\nIdentities are asserted over the corpus, not a draw. Algebraic invariances ((αA)x = α(Ax), (A + B)x = Ax + Bx, adjoint consistency ⟨Ax, y⟩ = ⟨x, Aᴴy⟩, transpose round trips) run over all test seeds, densities, and the adversarial patterns of the phase — a single random pattern demonstrates nothing about contention or accumulation-order bugs.\nNonfinite values follow SparseArrays: stored NaN/Inf are legal, participate in arithmetic, and propagate; no operation checks for them. Tests include patterns with nonfinite stored values and assert reference-matching propagation.\nBenchmarks land with the feature, keyed by representation, format, element type, and size.\nDocs land with the feature.\nRegression corpus (a convention, not part of the gates): every bug that escapes to a reported failure gets its minimal reproducer pinned as a permanent named test.\nShape and pattern edge cases are part of every gate: the empty matrix, a matrix with nnz = 0, fully dense stored patterns, a single stored entry, empty rows and columns, and stored entries whose value is zero (structural nonzeros holding 0 are preserved, matching SparseArrays).","title":"Cross-cutting requirements"},{"category":"section","location":"roadmap/#Non-goals","page":"Roadmap","text":"Stated so they are decisions, not omissions: no direct sparse factorizations (sparse LU/Cholesky/QR — Metal has no library support and the iterative path is the point of this package), and no Float64 on the device, which the hardware does not provide. Both remain available by converting to the CPU.","title":"Non-goals"},{"category":"section","location":"roadmap/#Phase-0-—-Foundations-*(complete)*","page":"Roadmap","text":"Package scaffolding, the test harness that validates every device result against the SparseArrays result for the same input, and the conventions for tolerances and reproducibility. Device test sets are skipped on machines without a GPU for local convenience only: CI requires the device (CI_EXPECT_DEVICE), so nothing merges unverified on Metal. Element types are discovered by probing the device.","title":"Phase 0 — Foundations (complete)"},{"category":"section","location":"roadmap/#Phase-1-—-Storage-formats-*(in-progress:-formats-and-interconversions-shipped;-dense-conversions-and-BSR-remain)*","page":"Roadmap","text":"Deliverables\n\nAbstractMtlSparseMatrix{Tv, Ti} <: SparseArrays.AbstractSparseMatrix{Tv, Ti}, and under it the device-resident formats, mirroring CUSPARSE naming:\nMtlSparseMatrixCSC{Tv, Ti} — colptr, rowval, nzval as MtlVectors plus dimensions, field names matching SparseMatrixCSC;\nMtlSparseMatrixCSR{Tv, Ti} — rowptr, colval, nzval;\nMtlSparseMatrixCOO{Tv, Ti} — rowval, colval, nzval;\nBSR later, once a consumer for it exists.\nConstructors from SparseMatrixCSC (converting indices to Ti with an overflow check against typemax(Ti)) and from raw index and value arrays with validation; MtlSparseVector deferred until a consumer needs it.\nAdapt.adapt_storage rules so adapt(MtlArray, A) and adapt(Array, dA) transfer in both directions; SparseMatrixCSC(dA) back-conversion.\nConversions between the three formats on the device, and conversion to and from dense arrays on both host (Array) and device (MtlArray).\n\nPhase gate\n\nRound trip host → device → host reproduces colptr, rowval, nzval, and the dimensions bit-for-bit, for every format, Tv, and Ti.\nEvery format conversion preserves the (i, j, v) triple set exactly, verified against reference by converting back to SparseMatrixCSC; CSC↔CSR round trips are exact; dense conversions agree with Array(::SparseMatrixCSC) and sparse(::Matrix) including the treatment of explicit zeros (densifying keeps values; sparsifying a dense array drops numerical zeros, matching sparse).\nInvalid construction is rejected with ArgumentError naming the violated invariant: non-monotonic pointer array, pointer not starting at one or ending at nnz + 1, indices out of range, unsorted or duplicate row indices within a column (policy matching SparseMatrixCSC), and index values exceeding typemax(Int32) on conversion.\nBase.show displays every format legibly at the REPL without scalar indexing (summary line plus a bounded number of entries fetched in one transfer).","title":"Phase 1 — Storage formats (in progress: formats and interconversions shipped; dense conversions and BSR remain)"},{"category":"section","location":"roadmap/#Phase-2-—-Array-interface","page":"Roadmap","text":"Deliverables\n\nsize, axes, eltype, similar (preserving and changing Tv/Ti), copy, collect, Array.\nnnz, nonzeros, rowvals (CSC), findnz returning host-usable triples.\nThe scalar getindex/setindex! policy: erroring under Metal.allowscalar(false), following the CUSPARSE precedent; documented.\nBroadcasting over stored values for zero-preserving scalar functions (A .* 2, abs.(A)); a function with f(0) ≠ 0 follows the CUSPARSE policy (error, do not densify silently).\nCOO assembly: sortperm on (j, i), duplicate accumulation with +, matching sparse(I, J, V, m, n, +).\nConstructor parity with SparseArrays: the constructors SparseArrays accepts work with device storage — sparse/format constructors from structured matrices over device vectors (Diagonal(::MtlVector), Bidiagonal, Tridiagonal, SymTridiagonal) and spdiagm-style construction. Also convenient for building test problems directly on device.\n\nPhase gate\n\nEvery listed method agrees with the reference exactly for structural output and to c·u for value output, across formats, Tv, and Ti.\nStructured-matrix constructors agree exactly (structure and values) with sparse applied to the same structured matrix over host vectors.\nsimilar variants produce the documented format, element type, and index type; collect/Array equal Array(SparseMatrixCSC(dA)) elementwise.\nBroadcasting: zero-preserving functions preserve the pattern exactly (stored zeros included); non-zero-preserving functions raise the documented error; the whole broadcast suite runs under allowscalar(false).\nCOO assembly with duplicate, unsorted, and adversarially ordered input (reverse-sorted, all-duplicates collapsing to one entry, interleaved columns) matches sparse(I, J, V, m, n, +) exactly in structure and to c·k·u in values, k the largest duplicate multiplicity.","title":"Phase 2 — Array interface"},{"category":"section","location":"roadmap/#Phase-3-—-Sparse-BLAS","page":"Roadmap","text":"Deliverables\n\nmul! in three- and five-argument forms: sparse × dense vector and sparse × dense matrix, for each storage format; Transpose, Adjoint, Symmetric, and Hermitian wrappers.\nSparse triangular solve (ldiv! on UpperTriangular/LowerTriangular-wrapped device sparse matrices, unit and non-unit diagonal) — placed here for completeness of the solver interface and needed by Phase 6's incomplete factorizations.\nDispatch to Metal Performance Shaders where a matching primitive exists, hand written kernels otherwise; a stated, tested policy on reproducibility of atomic accumulation (deterministic ordering preferred; where atomics are used the docstring says so).\n\nPhase gate\n\nSpMV/SpMM: |y_device − y_ref| ≤ c·r·u·(|A|·|x|) componentwise, r the maximum stored entries per row — the bound that stays honest for long accumulations; in Float16/BFloat16 a widened accumulator meets the same bound where the docstring promises it.\nFive-argument semantics exact to convention: β = 0 overwrites C even when C contains NaN; α = 0 skips the product; both tested explicitly.\nWrapper variants agree with materialized reference computations (transpose(A) * x against reference on the same triples).\nAdversarial patterns: empty rows and columns, one fully dense row (longest accumulation), one fully dense column (maximal write contention for CSR/COO atomics), diagonal, permutation matrices, laplacian_2d, and patterns sized to straddle kernel launch boundaries.\nReproducibility: where determinism is promised, bit-identical results over repeated runs on the same device; where it is not, the docstring says so and the test asserts the accuracy bound instead.\nTriangular solve: componentwise backward error ≤ c·n·u on well conditioned triangles; graded triangles (diagonal spanning u … 1/u) meet the normwise bound or fail loudly, never silently.","title":"Phase 3 — Sparse BLAS"},{"category":"section","location":"roadmap/#Phase-4-—-Iterative-solver-compatibility","page":"Roadmap","text":"This comes directly after sparse BLAS — not after the structural operations — because it needs almost nothing beyond five-argument mul! and is the package's headline promise.\n\nDeliverables\n\nConformance to the operator interface Krylov.jl requires (mul!, eltype, size, adjoint application) for every storage format.\nAn end to end sweep: cg and minres on laplacian_2d; gmres and bicgstab on a nonsymmetric convection–diffusion operator; lsmr on a tall least squares problem — each with a known solution.\n\nPhase gate\n\nEach solver reaches its residual tolerance on the device with an iteration count within a factor of two of the same solver on the CPU reference matrix (identical mathematics, different rounding — iteration counts may differ slightly, not qualitatively).\nRecovered solutions match the constructed true solutions to the tolerance implied by the solver's stopping criterion and κ of the problem.\nNo scalar indexing during any solve (allowscalar(false)); device allocations per iteration are bounded and asserted, so a solver loop does not silently allocate per step.\nExit criterion: an unmodified Krylov.jl example runs on a device matrix by changing only the array types. This example is a doctest in the documentation.","title":"Phase 4 — Iterative solver compatibility"},{"category":"section","location":"roadmap/#Phase-5-—-Structural-and-arithmetic-operations","page":"Roadmap","text":"Deliverables\n\nTranspose and adjoint materialization; row and column permutation.\n+, -, scalar multiplication and division; sparse–sparse multiplication.\nkron, hcat/vcat/hvcat, dropzeros!, droptol!.\nNorms (norm, opnorm(A, 1), opnorm(A, Inf)) and reductions (sum, maximum/minimum over stored entries with the documented empty-row policy).\n\nPhase gate\n\nStructural operations (transpose, permutation, kron, concatenation, dropzeros!) match the reference pattern exactly.\nArithmetic: values to c·r·u bounds as in Phase 3; pattern-union cases where addition cancels to numerical zero keep the stored entry, exactly as SparseArrays does.\ndroptol! agrees with reference on entries straddling the threshold, including ties.\nSparse–sparse products validated on patterns that maximize fill (outer-product shaped factors) and on patterns with no overlap (empty result).","title":"Phase 5 — Structural and arithmetic operations"},{"category":"section","location":"roadmap/#Phase-6-—-Preconditioners","page":"Roadmap","text":"Deliverables\n\nJacobi and block Jacobi preconditioners; ic0 and ilu0 incomplete factorizations, built on the Phase 3 sparse triangular solve.\nThe application interface KrylovPreconditioners.jl expects (ldiv!-style two- and three-argument application).\n\nPhase gate\n\nic0/ilu0 factors match a straightforward CPU reference implementation of the same zero-fill algorithm: identical pattern, values to c·n·u — validated entry by entry, not just through the solve.\nBreakdown (zero or negative pivot in ic0, zero pivot in ilu0) raises a precise error naming the pivot, matching the reference implementation's behavior on the same input.\nEffectiveness, not just correctness: preconditioned cg on laplacian_2d reduces iteration count versus unpreconditioned by the factor the literature leads one to expect for ic0 at that mesh size, asserted with a documented margin — a preconditioner that \"works\" but does not accelerate fails its gate.\nA KrylovPreconditioners.jl example runs unmodified with these types.","title":"Phase 6 — Preconditioners"},{"category":"section","location":"roadmap/#Phase-7-—-Performance-and-release","page":"Roadmap","text":"Deliverables\n\nKernel optimization guided by the measured crossovers; format-selection guidance in the documentation backed by the benchmark table.\nREPL quality-of-life display matching SparseArrays: the pattern/entry rendering SparseMatrixCSC gives at the REPL (braille pattern preview and the truncated entry listing), produced from a bounded number of entries fetched in one host transfer — never by scalar indexing.\nComparison against the documented behavior of CUDA.CUSPARSE for API parity.\nComplete documentation with doctested examples; registration in General.\n\nPhase gate\n\nThe performance goals below hold for every shipped operation, evidenced by the benchmark crossover table checked into the docs.\nAn API parity checklist against SparseArrays and CUSPARSE for every shipped feature, with every deliberate deviation documented in the docstring.\nAqua piracy check clean; every public name docstringed and doctested; the registration PR passes General's automerge checks.","title":"Phase 7 — Performance and release"},{"category":"section","location":"roadmap/#Phase-8-—-Autodiff-compatibility","page":"Roadmap","text":"Differentiation support, added last because the rules are written against operations that must first be stable.\n\nDeliverables\n\nChainRulesCore rules for the linear operations — SpMV, SpMM, and the solves through preconditioners — where the subtlety is not the derivative (the maps are linear) but the projection: gradients with respect to a sparse argument are projected onto its sparsity pattern, mirroring ChainRules' ProjectTo(::SparseMatrixCSC) semantics, and computed without scalar indexing so the same rule serves the device.\nRules for structure-preserving operations (scaling, +, transpose) and a documented policy for operations whose derivative would densify.\n\nPhase gate\n\nChainRulesTestUtils.test_rrule/test_frule pass for every rule with the primal on the CPU mirror, finite differences to the element type's tolerance.\nThe same rules execute on the device with gradients matching the CPU-path gradients to c·r·u, pattern preserved exactly.\nAn end to end gradient through a Krylov.jl solve (differentiating x(b) for fixed A, the adjoint-solve identity) matches the CPU result — the integration test that the pieces compose.","title":"Phase 8 — Autodiff compatibility"},{"category":"section","location":"roadmap/#Performance-goals","page":"Roadmap","text":"Benchmarks live in benchmarks/ and are direct measurements of performance, not proxies. Two standing targets:\n\nDevice sparse operations run faster than the same operations on SparseArrays on the CPU once the problem size is large enough, with the crossover size measured and reported per operation, format, and element type.\nDevice sparse operations beat dense MtlArray operations on the same problem starting at a fairly modest size — sparsity must pay for itself well before problems become huge, or the format or kernel needs rework.","title":"Performance goals"},{"category":"section","location":"#MetalSparseArrays.jl","page":"Home","text":"Sparse arrays for the Apple Silicon GPU backend. Code written against SparseArrays on the CPU, or against CUDA.CUSPARSE on NVIDIA hardware, runs unchanged on Metal — same storage formats, same function names, same semantics — including the operator interface that iterative solvers such as Krylov.jl require.\n\nThe target API is the union of two existing, well tested APIs. Almost nothing here is a new design; the work is faithful reimplementation on a different device, and reuse of the validation those libraries already provide.","title":"MetalSparseArrays.jl"},{"category":"section","location":"#Getting-started","page":"Home","text":"The package is registered nowhere yet; install it by developing the repository:\n\nusing Pkg\nPkg.develop(url = \"https://github.com/jeremyiwk/MetalSparseArrays.jl\")\nusing MetalSparseArrays\n\nAn Apple Silicon GPU is required for device operations. The test suite runs anywhere for local development — device test sets are skipped without a GPU — but merges are gated on CI runs where a Metal device is required to be present and exercised, so no functionality lands unverified on device.","title":"Getting started"},{"category":"section","location":"#Device-constraints","page":"Home","text":"These are properties of the hardware and shape every design decision:\n\nNo double precision. Apple GPUs support Float16, BFloat16, Float32, and the complex types built on each — but not Float64, which appears only as the element type of CPU references. Supported element types are probed at run time.\n32-bit indices. The default index type is Int32, matching CUSPARSE; conversions from host matrices are checked against typemax(Int32).\nNo scalar indexing of device arrays in library code.\nAsynchrony. Kernel launches return before the work completes; results are synchronized before host reads.\n\nCorrectness is defined by the CPU reference: every device operation is validated against the SparseArrays result for the same input — exactly for structural results, and to a stated floating point bound otherwise.\n\nSee the Roadmap for the phase ordering of the work and the API reference for what is implemented today.","title":"Device constraints"}]
}
